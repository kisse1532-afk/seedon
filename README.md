## 씨드온 (SeedOn)

이미 존재하지만 닿지 않는 지원을, 청소년이 눈치 보지 않고 쓸 수 있게 연결하는 플랫폼입니다.

- 프론트엔드: Next.js (App Router)
- DB: Supabase (Postgres)
- 배포: Vercel (main 브랜치 push 시 자동 배포)

### 개발

```bash
npm install
npm run dev
```

### 빌드 확인

```bash
npm run build
```
