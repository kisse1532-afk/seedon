# 씨드온 (SeedOn) — 프로젝트 매뉴얼 (에이전트 운영 원칙)

> 2026.08.11 로드 작성, 2026.08.12 절대규칙·GitHub 연동 현황 반영해 갱신.
> 데일리 헬스체크+개발 에이전트는 매 실행 시 이 파일을 먼저 읽을 것.
> 노션 MVP 현황 페이지("씨드온 — MVP 현황 & 다음 액션")와 이 파일이 다르면, **이 파일과 실제 코드/배포 상태가 우선**이다. 노션 기록은 참고용이며 무조건 신뢰하지 않는다.

## 절대규칙 (반드시 지킬 것)

1. **낙인 문구 금지.** "저소득층", "지원대상자", "취약계층", "결손가정", "차상위계층", "수급자" 같은 표현을 화면·문구 어디에도 쓰지 않는다.

   | 금지 | 대체 |
   |---|---|
   | 저소득층 | "이 프로그램에 해당하는 분" |
   | 지원대상자 | "신청 가능한 분" |
   | 취약계층 | (표현 자체를 쓰지 않고 조건만 서술) |
   | 결손가정 / 차상위계층 / 수급자 | 구체적 소득분위·조건 수치로만 서술 (예: "중위소득 O% 이하") |

   화면별 원칙:
   - **카드/리스트**: 프로그램명·기관명·설명만 노출. 대상 조건은 "신청 전 안내"에서만 펼쳐 보이게(기본 접힘)
   - **신청 폼**: 이름·연락처 외 항목 없음. 소득분위 등은 자가진단 문구("해당될 것 같으면 신청해보세요")로 우회
   - **완료/알림 문구**: "지원 대상으로 확인되어..." 같은 판정형 문장 금지, "신청이 접수됐어요" 톤 유지
   - **관리자 화면**: 내부용이라도 신청자 목록에 낙인성 라벨(예: "저소득 O명")을 달지 않는다. 카테고리·프로그램명 기준으로만 집계

2. **씨드온이 소득분위·급식카드 인증 등 낙인 유발 민감정보를 요구하지 않는다.** (나비얌의 "급식카드 인증하면 혜택 와르르" 같은 패턴은 벤치마킹하되 가져오지 않음)

   **이 규칙이 적용되는 대상은 "씨드온이 만드는 화면·폼·기능"이다. 외부 지원제도의 자격요건이 아니다.**
   현실에서 공공 제도 상당수가 차상위·기초생활수급 같은 자격요건을 두고 있고, 그건 씨드온이 바꿀 수 있는 게 아니다. **오히려 그런 제도일수록 존재를 알려주는 게 정보 비대칭 해소라는 씨드온의 미션 그 자체다.** 자격요건이 있다는 이유로 프로그램을 목록에서 빼지 말 것.
   - ✅ 등록해도 됨: 자격요건에 소득 조건·급식카드 인증이 있는 외부 프로그램 (예: 나비얌 — 2026.08.12 로드 확정)
   - ❌ 만들면 안 됨: 씨드온 화면에서 청소년에게 소득분위를 묻거나, 급식카드를 인증시키거나, 자격을 판정해주는 기능
   - 단, 그 조건을 **카드·리스트에 라벨로 노출하지는 않는다** — 위 1번 화면별 원칙대로 "신청 전 안내"에서 기본 접힘으로, 수치 조건으로만 서술

3. **파일럿·서비스 대상은 14세 이상으로 한정한다.** (14세 미만은 법정대리인 동의가 필요해 복잡도가 커짐 — 프로그램 등록 시에도 이 기준으로 대상 연령 확인)

4. **1단계 큐레이션 → 2단계 직접지원 확장 가능성 (무신사 스탠다드 모델).** 씨드온은 지금 "정보/매칭 플랫폼(기존 지원제도 연결)" 단계다. 무신사가 편집숍(1단계)에서 자체 브랜드 운영(2단계, 무신사 스탠다드)으로 확장했듯, 씨드온도 검증 이후 "자체 지원 프로그램 운영"으로 확장할 가능성이 있다. 지금 단계에서 미리 2단계 기능을 만들지 말 것 — MVP 본체(매칭·큐레이션)는 AI 에이전트가 상시 운영하고 로드는 최소 개입만 하는 구조를 유지하며, 확장은 실증 데이터가 쌓인 뒤 로드가 결정한다.

## 회사 소개
씨드온은 청소년 지원 정보 플랫폼이다. 핵심 미션: "이미 존재하지만 닿지 않는 지원을, 청소년이 눈치 보지 않고 쓸 수 있게 연결하여 한 청소년의 삶을 바꾸자"

핵심 가설: "개천에서 용이 나려면 정보 비대칭부터 없애야 한다" — 교육불평등의 상당 부분은 "기회가 없어서"가 아니라 "이미 있는 지원제도를 몰라서/신청이 번거로워서/낙인이 두려워서" 못 쓰는 것이다.

## 스코프 원칙 (중요)
씨드온의 역할은 "정보 비대칭과 접근 장벽 제거"까지다. 접근 이후 청소년의 삶이 어떻게 되는지(결과)는 씨드온의 관할이 아니다. 이 선을 넘어서 기능을 확장하려 하지 말 것.

## 기술 스택
- 프론트엔드: Next.js (유지)
- 백엔드: Next.js API Routes → Python FastAPI로 전환 진행 (승인 절차 없이 바로 진행. 단, "되돌리기 원칙" 반드시 지킬 것)
- DB: Supabase (Postgres 17) → Python 전환 부분은 supabase-py 클라이언트 사용
- 배포: Vercel — **GitHub 저장소(kisse1532-afk/seedon)의 `main` 브랜치에 직접 연결되어 있어, `main`에 머지되면 자동으로 빌드·배포된다** (2026.08.12부터, `deploy_to_vercel` 수동 파일 업로드 방식은 폐기)
- Vercel 프로젝트 ID: prj_YzCpQbREKL2Lagz3kQByEP6QDIvt / 팀 ID: team_vgXtix70gk816WMIqyI7vVLg
- Supabase 프로젝트 ID: ssxxqiwlywcgmgkdgmtz

## 배포 방식 (2026.08.12 확정)
- 작업은 `claude/seedon-status-diagnosis-1urtj4` 브랜치(또는 기능별 새 브랜치)에서 진행하고 커밋마다 `git push`
- `main`으로의 직접 push는 브랜치 보호로 막혀 있으므로, GitHub PR을 만들어(`mcp__github__create_pull_request`) 머지(`mcp__github__merge_pull_request`)하는 방식으로 반영
- 머지되면 Vercel이 자동으로 빌드·배포하므로, 몇 분 뒤 `get_deployment`로 READY 확인 후 실제 URL(`web_fetch_vercel_url` 등)을 열어 직접 검증할 것

## FastAPI 전환 원칙 (나중에 또 갈아탈 수 있게)
1. 비즈니스 로직(매칭, 신청 처리 등)은 /core, /services 같은 별도 모듈로 분리하고, FastAPI 라우터는 그걸 호출만 하는 얇은 계층으로 유지
2. DB 접근은 한 곳(/db, repository 패턴)으로 모아서 나중에 DB를 바꿔도 그 계층만 손대면 되게
3. API 스펙은 OpenAPI/Swagger로 자동 문서화
4. FastAPI 전용 문법은 얇은 어댑터 계층에만 쓰고 핵심 로직에는 안 섞이게
5. 진단(어떤 기능이 실제로 작동하는지)과 전환을 같은 기능 단위로 묶어서 진행 — 예: "로그인 기능 진단 + 전환"을 한 세트로, 로그인 다 되면 다음 기능으로

## 현재 최우선 목표
MVP를 "실제로 써볼 수 있는 상태"로 만드는 것. 순서: 진단(+ FastAPI 전환 병행) → 기본 기능 복구 → 신청하기 플로우 완성 (카드 → 신청버튼 → 최소정보 입력 → 원클릭 제출 → 완료화면)

이 신청하기 플로우가 완성돼야 핵심 파일럿 지표(카드 조회 → 신청 클릭 → 폼 제출 완료 → 실제 이행)를 측정할 수 있다. 이게 지금 가장 중요한 일이다.

## 검수 규칙 (반드시 지킬 것)
1. 다 됐다고 보고하기 전에 결과물을 직접 열어서 확인하고 증거를 보여줄 것
2. 확인 못 한 건 못했다고 그대로 말할 것
3. 노션에 "완료"로 기록되어 있어도 실제로 작동 안 하면 안 되는 대로 보고할 것 — 노션 기록을 무조건 신뢰하지 말고 실제 코드/배포 상태로 확인할 것

## 진행 방식 원칙
- MVP 단계이므로 작업은 승인 절차 없이 바로 진행한다 (기능 개발, 버그 수정, 백엔드 전환 포함)
- 단, 아래 "되돌리기 원칙"을 반드시 지켜서 언제든 이전 상태로 복구 가능하게 할 것
- 데이터 손실 가능성이 있는 스키마 변경(컬럼 삭제 등)·삭제성 변경은 예외적으로 로드 컨펌을 먼저 받을 것. 순수 추가(nullable 컬럼 추가 등)는 승인 없이 진행 가능
- "Do things that don't scale" — 과도한 엔지니어링, 불필요한 리팩토링은 지양하고 핵심 기능 작동에 집중할 것
- 진행 상황, 진단 결과, 결정사항은 노션 MVP 현황 페이지에 날짜별로 기록할 것

## 되돌리기 원칙
1. 작업 하나(기능 하나, 개선 하나)마다 별도 git 커밋으로 나눠서 저장할 것 — 여러 변경을 한 커밋에 몰아넣지 말 것
2. 커밋 메시지에 "무엇을 왜 바꿨는지" 명확히 남길 것
3. 로그인/OAuth, 결제, 개인정보 관련 로직처럼 되돌리기 어렵거나 위험한 부분을 건드릴 때는 특히 신중하게, 커밋을 더 잘게 쪼개서 작업할 것
4. 바꾼 게 기존 기능을 깨뜨렸다면, 로드가 "전 단계로 돌려줘"라고 하면 즉시 해당 커밋을 되돌릴 수 있어야 함 — 실제로 되돌리는 게 가능한 상태를 항상 유지하며 작업할 것
5. 하루 작업 끝나면 "오늘 바뀐 것 목록"을 정리해서 로드가 원하면 특정 항목만 골라서 되돌릴 수 있게 브리핑할 것

GitHub 연동은 2026.08.11 정상화됐고, 2026.08.12 Vercel-GitHub 자동배포까지 연결 완료되어 이 원칙이 실제로 작동한다.

## 매일 할 일 (개발자 에이전트)
1. 헬스체크 — 로그인(이메일/Google/Kakao), AI 맞춤추천, seedon-daily-research 파이프라인이 정상 작동하는지 확인
2. 에러 로그 점검 — Vercel/Supabase 로그에 새로 발생한 에러 있는지 확인
3. seedon-daily-research 결과 검증 — 등록된 프로그램 데이터가 정상인지 샘플 확인 (공식 링크 필수, 없으면 published 금지)
4. 벤치마크 앱(품/나비얌/유스보이스 등) 리뷰하고 씨드온 적용 인사이트 정리 (주 1회)
5. 위에서 나온 개선 아이디어는 승인 절차 없이 바로 진행 (되돌리기 원칙 지키며, 데이터 손실 가능성 있는 변경은 컨펌 먼저)
6. 오늘 확인한 것 / 고친 것 / 새로 시도한 것을 노션에 날짜별로 기록, 로드에게 브리핑

## 커뮤니케이션 규칙
- 로드는 비개발자다. 전문 용어가 나오면 쉽게 풀어서 설명할 것
- 로드가 확정하지 않은 아이디어는 "가설/초안"으로 취급하고, 로드가 확정한 것만 "결정사항"으로 기록할 것

## 팀 구조 (2026.08.12 확정)
지금은 개발자 역할 하나에만 집중한다. 기획/마케팅/CS/디자인 등 부서 분할, Claude Code Agent Teams 활성화, 신규 자동화 스케줄, B2G/SEO 수익화 착수는 모두 보류 — MVP 기본 기능(로그인/AI추천/신청플로우)이 실제로 검증된 뒤 로드가 다시 논의해서 결정한다. 노션 "씨드온 — 팀 구조 & 운영 원칙" 페이지의 6개 팀 체제는 향후 참고용 설계일 뿐, 지금 가동 중이 아니다.

## 벤치마크 (참고용)
비투비(품), 유스보이스, 프로젝트비욘드, 나눔비타민(나비얌), 레어노트(휴먼스케이프), 코워크 → 공통 패턴: 실제 traction이 법인 설립보다 먼저였고, 초기엔 자원이 부족했으며, "이미 있는 걸 왜 사람들이 못 쓰는가"를 핵심 문제의식으로 삼았다.
